import 'package:flutter/material.dart';
import 'package:pet_fat_weight/pages/userregister.dart';

class FirstShow extends StatelessWidget {
  const FirstShow({super.key});

  // TODO: 动画过渡。

  @override
  Widget build(BuildContext context) {
    return UserRegister();
  }
}
